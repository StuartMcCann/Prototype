@model Prototype.Models.CandidateProfile

<div class="container text">

    <h1 class="heading">Keep You Details Updated Here</h1>

    <div class="row">

        <div class="col-sm">
            <h3>Your Current Details</h3>

            <h5>Job Title: @Model.JobTitle</h5>
            <h5>Requested Rate: @Model.Rate</h5>
            <!--ADD LOGIC HERE FOR RATE ADVISE-->
            <h5>Current Availablity: @if (Model.IsAvailable)
            {<span>Available For Contracts</span>}
            else
            {<span>Not AvailableFor Contracts</span>}</h5>
            <h5>Available Date: @Model.AvailableFrom.ToShortDateString()</h5>
            <h5>Current Level: @Model.Level</h5>
            <h5>Skills</h5>
            <h5>
                @foreach (var skill in Model.Skills)
                {
                    <span class="badge skill">@skill.SkillName</span>

                }
            </h5>




        </div>
        <div class="col-sm">
        <h3>Actions</h3>

        <form asp-action="UpdateCandidateSkill" asp-controller="Candidate" method="post" enctype="multipart/form-data">
            @*<div class="mb-3">
            <label >Skills</label>
            @Html.ListBoxFor(m => m.SkillIds, (SelectList)ViewBag.Skills, htmlAttributes: new { @class = "form-control" })
            <span asp-validation-for="Skills" class="text-danger"></span>
        </div>*@
            @Html.DropDownList("skillIds", new SelectList(ViewBag.Accounts, "SkillId", "SkillName"),  new { @class = "form-control" }))

            <button type="submit" class="btn btn-primary"><i class="fas fa-plus"></i> Add</button>
        </form>
        
        
        
        </div>









    </div>






























</div>